<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="portfolio">
	<insert id="insertUploadPortfolio" parameterType="PortfolioDTO">
		INSERT INTO PORTFOLIO(HUMAN_ID, SUBJECT, CONTENT, MAKE_DT, SORT, NUMBER_PEOPLE, MADE_PEOPLE, COMPLETE, IMAGE)
		
		VALUES(
			#{humanId},
			#{subject},
			#{content},
			#{makeDt},
			#{sort},
			#{numberPeople},
			#{madePeople},
			#{complete},
			#{image}
		
		
		)
	</insert>


</mapper>